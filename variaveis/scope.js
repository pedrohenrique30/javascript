// Scope
// Escopo determina a visibilidade de alguma variavel no JS

// Block statement

// vamos iniciar um bloco
{

    // aqui dentro é um bloco e posso colocar qualquer codigo

} // aqui fechamos o bloco

// O bloco, tambem irá criar um novo escopo. Chamamos de block-scoped

// O que é o escopo dentro do javascript?
// O escopo é o formato de encapsular(deixar dentro) valor em um local. O contexto daquela função/local vai determinar a acessibilidade das variáveis ali.